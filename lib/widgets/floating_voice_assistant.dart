import 'package:flutter/material.dart';

class FloatingVoiceAssistant extends StatelessWidget {
  const FloatingVoiceAssistant({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
